services:
  jaci:
    build: .
    container_name: jaci
    restart: unless-stopped
    ports:
      - "8003:8000"
    env_file:
      - ${ENV_FILE:-.env.production}
    volumes:
      - /opt/jaci/backups:/var/backups/jaci
    labels:
      - "dozzle.monitor=true"
