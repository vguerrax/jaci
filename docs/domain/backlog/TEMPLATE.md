# BL-NNNN — Título curto

> Copie para `docs/domain/backlog/items/BL-NNNN-slug.md`, substitua os
> placeholders e remova esta orientação. A especificação técnica detalhada
> pertence ao refinamento.

## Identificação e entrada

| Campo | Valor |
| --- | --- |
| ID | `BL-NNNN` |
| Título | `<título curto>` |
| Tipo | `<bug | ajuste | melhoria>` |
| Estado | `recebido` |
| Severidade | `a_triar` |
| Prioridade | `a_definir` |
| Data de entrada | `<AAAA-MM-DD>` |
| Origem | `<canal, pessoa, rotina ou referência>` |
| Ambiente/versão | `<produção, homologação, versão ou commit>` |
| Responsável | `<nome, equipe ou a_definir>` |
| Atualizado em | `<AAAA-MM-DD>` |

### Comportamento observado

_Descreva a situação atual._

### Comportamento esperado

_Descreva o resultado desejado sem antecipar a solução técnica._

### Impacto e abrangência

- Impacto: `<efeito para usuários, operação, dados ou negócio>`
- Abrangência: `<grupos, perfis, registros ou fluxos afetados>`
- Frequência: `<sempre | frequente | intermitente | isolado | desconhecido>`

### Passos de reprodução

1. `<pré-condição ou ação>`
2. `<ação>`
3. `<resultado observado>`

Quando não for aplicável ou conhecido, registre o motivo.

### Evidências sanitizadas

- `<link, identificador seguro ou descrição>`

Nunca inclua credenciais, tokens, cookies, chaves, dados pessoais ou dados de
outro grupo.

### Workaround

_Informe a alternativa temporária e seu impacto, ou "não conhecido"._

## Triagem

| Campo | Valor |
| --- | --- |
| Confirmação | `<confirmado | parcial | nao_reproduzido | nao_aplicavel | a_triar>` |
| Classificação | `<causa aparente, natureza da demanda ou a_triar>` |
| Domínio afetado | `<domínio, fluxo ou componente>` |
| Regras de negócio afetadas | `<identificadores ou a_triar>` |
| Risco de segurança | `<sim/não/a_avaliar + justificativa>` |
| Risco de LGPD | `<sim/não/a_avaliar + justificativa>` |
| Risco de isolamento por grupo | `<sim/não/a_avaliar + justificativa>` |
| Risco offline/sincronização | `<sim/não/a_avaliar + justificativa>` |
| Risco ao histórico financeiro | `<sim/não/a_avaliar + justificativa>` |
| Risco à recorrência | `<sim/não/a_avaliar + justificativa>` |
| Risco à separação template/execução | `<sim/não/a_avaliar + justificativa>` |
| Dependências | `<itens, sistemas, pessoas ou a_triar>` |
| Duplicidades | `<link para BL relacionado ou a_triar>` |
| Responsável pela próxima etapa | `<nome, equipe ou a_definir>` |
| Próximo passo | `<ação objetiva>` |

## Acompanhamento até produção

- Documento refinado: `<link ou ainda não criado>`
- Implementação (commits/PRs): `<links ou ainda não iniciada>`
- Validações: `<links, comandos/resultados ou ainda não iniciadas>`
- Publicação: `<ambiente, versão, data e referência ou ainda não publicada>`
- Itens relacionados: `<links ou nenhum>`

### Impedimentos

- `<nenhum>`

## Histórico

| Data/hora | Autor | Mudança | Motivo/evidência |
| --- | --- | --- | --- |
| `<AAAA-MM-DD HH:MM TZ>` | `<autor>` | Item criado em `recebido` | `<origem>` |
